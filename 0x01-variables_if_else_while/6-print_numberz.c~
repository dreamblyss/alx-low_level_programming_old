#include <stdio.h>
#include <stdlib.h>

int main(void){

  char charVal;
  int num;

 /**

  * main - Entry point

  *

  * Return: Always 0 (Success)

  * 

  */

  int num;

  for(num = 0; num < 10; ++num)
  {
    charVal = num + '0';
    putchar(charVal);
  }

  putchar(0xA);

  return(0);

}
